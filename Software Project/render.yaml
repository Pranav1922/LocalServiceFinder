services:
  - type: web
    name: local-services-finder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn lsf_app:app
    envVars:
      - key: JWT_SECRET
        generateValue: true
